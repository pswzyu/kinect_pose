#include "gaussianofjoint.h"


GaussianOfJoint::GaussianOfJoint()
{
//    hidden_card = p_hidden_card;
//    mu_y = zeros<vec>(hidden_card);
//    sigma_y = zeros<vec>(hidden_card);
//    mu_x = zeros<vec>(hidden_card);
//    sigma_x = zeros<vec>(hidden_card);
//    mu_angle = zeros<vec>(hidden_card);
//    sigma_angle = zeros<vec>(hidden_card);

    // in clg, mu_y,mu_x,mu_angle is depend on parent's
    // x,y,angle, plus a bias term, 12  = 3*(3+1)
//    theta = zeros<mat>(hidden_card, 12);
}
